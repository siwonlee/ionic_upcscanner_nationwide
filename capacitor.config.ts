import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.joonspa.siwon2024',
  appName: 'APL Checker Nationwide',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
